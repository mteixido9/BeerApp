# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'one2one-Beer-App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for one2one-Beer-App

  target 'one2one-Beer-AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'one2one-Beer-AppUITests' do
    # Pods for testing
  end

end
